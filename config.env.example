# Copy this file to `config.env` and fill the values (do NOT commit your real credentials)
# MongoDB connection string. Use <password> placeholder for the password token shown in the connection string.
DB_URL=mongodb+srv://srikanth123:srikanth@cluster0.14wpssx.mongodb.net/
# The actual password to replace <password> in DB_URL
PASSWORD=your_db_password_here
# Optional: port to run server
PORT=6000
# Gmail app password for sending OTP emails (use App Passwords for Gmail)
MAIL_PASS=your_gmail_app_password_here
